import { TextStyle, ViewStyle } from "react-native"

export const emptyStateStyles = {
  WRAPPER: {
    justifyContent: "center",
    display: "flex",
    alignItems: "center",
    flex: 1,
    padding: 32,
  } as ViewStyle,
  SUBHEADING: {
    textAlign: "center",
  } as TextStyle,
}
