import { TimelineStoreModel } from "./timeline-store";
test("can be created", () => {
    const instance = TimelineStoreModel.create({});
    expect(instance).toBeTruthy();
});
//# sourceMappingURL=timeline-store.test.js.map